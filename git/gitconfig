[user]
	name = Gary Blackwood
	email = gareeblackwood@gmail.com
[core]
	editor = emacs
[color]
	ui = true
[alias]
	co = checkout
	a = add
        aa = add --all
	c = commit
	s = status
	b = branch
        d = diff --color
	l = log --pretty=format:'%h %ad | %s%d [%an]' --graph --date=short
        pom = push origin master
        pod = push origin develop
        pomf = push origin master --force
        podf = push origin develop --force
